# ERPNext Kamal Secrets
# DO NOT COMMIT ACTUAL CREDENTIALS - use environment variables or secret managers

# Docker Hub registry password
KAMAL_REGISTRY_PASSWORD=$DOCKERHUB_TOKEN

# Database passwords
# Generate strong passwords: openssl rand -base64 32
MYSQL_ROOT_PASSWORD=$MYSQL_ROOT_PASSWORD

# Backup service password (same as MYSQL_ROOT_PASSWORD)
MYSQL_PASS=$MYSQL_ROOT_PASSWORD

# ERPNext admin password
ADMIN_PASSWORD=$ERPNEXT_ADMIN_PASSWORD

# Example environment setup (add to your shell profile or .env file):
#
# Server configuration (REQUIRED)
# export PRIMARY_HOST="192.168.1.10"      # Server IP
# export ERPNEXT_DOMAIN="erp.example.com" # Your domain
# export SSH_USER="root"                  # SSH user for deployment
#
# Docker Hub (REQUIRED)
# export DOCKER_USERNAME="your-dockerhub-username"
# export DOCKER_IMAGE="your-dockerhub-username/erpnext"
# export DOCKERHUB_TOKEN="your-docker-hub-token"
#
# Database passwords (REQUIRED)
# export MYSQL_ROOT_PASSWORD="$(openssl rand -base64 32)"
#
# ERPNext (REQUIRED)
# export ERPNEXT_ADMIN_PASSWORD="your-admin-password"
